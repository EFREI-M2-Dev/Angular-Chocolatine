import { AnswerTimePipe } from './answer-time.pipe';

describe('AnswerTimePipe', () => {
  it('create an instance', () => {
    const pipe = new AnswerTimePipe();
    expect(pipe).toBeTruthy();
  });
});
